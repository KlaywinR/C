#include <stdio.h>
int main(void)
{
  /**
   * Implemente um programa em linguagem C que exiba em tela todos os números primos existentes
 entre 1 e 120. Lembre-se que um número primo é um número natural maior que 1 que só é divisível
 por 1 e por ele mesmo.
   */
  int i, j, primo;
  for (i = 2; i <= 120; i++)
  {
    primo == 2;
  }
  for (j = 2; j < primo; j++)
  {
  }

  return 0;
}